: compile load
@echo -n 'Checking for socket libraries: '
@echo 'main() { ; }' >trylib.c
@( ( ./compile trylib.c && ./load trylib -lsocket -lnsl ) >/dev/null 2>&1 \
  && echo -lsocket -lnsl || exit 0 ) >socket.lib
@rm -f trylib.c trylib.o trylib
